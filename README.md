# MhDownload
这是一个多线程下载器</br>
打开mhdown.py或前往releases下载exe运行即可</br>
若运行源代码，请先执行以下命令安装requests库和pystray库:</br>
pip install requests</br>
pip install pystray</br>
使用以下命令默认隐藏窗口:</br>
python mhdown.py --hide</br>
mhd.exe --hide</br>
